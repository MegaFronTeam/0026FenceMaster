mixin item-sos(icon, a='#')
	+e.A.item&attributes(attributes)(href= a target="_blank")
		if block
			block
		else
			+icon(icon)
//- social buttons
mixin soc()
	+b.soc
		if block
			block
		else
			each soc in social
				+item-sos(soc)

//- /social buttons 